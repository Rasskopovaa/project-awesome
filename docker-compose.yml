version: '3'
services:
  db:
    image: "postgres"
    ports:
      - "5432:5432"
    container_name: db
    environment:
      POSTGRES_DB: 'BookShop'
      POSTGRES_USER: 'postgres'
      POSTGRES_PASSWORD: 'postgres'